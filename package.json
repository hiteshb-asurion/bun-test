{
  "name": "bun-test",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/webapp"
  ],
  "scripts": {
    "dev:api": "bun --watch apps/api/src/index.ts",
    "dev:webapp": "cd apps/webapp && bun run dev",
    "dev": "bun run dev:api & bun run dev:webapp"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
